version: "3.8"

services:
  app:
    container_name: liqitransactions_app
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src

networks:
  financial-network:
    driver: bridge
